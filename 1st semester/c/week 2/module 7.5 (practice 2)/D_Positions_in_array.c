#include <stdio.h>
int main()
{
    int n, i;

    scanf("%d", &n);

    int a[n];

    for (i = 0; i < n; i++)
    {
        scanf("%d", &a[i]);
    }

    for (i = n; i > n; i--)
    {
        if(i % == 0)
        {
            printf("%d ", a[i]);
        }
    }

    return 0;
}
